<ListBox x:Class="WindowsPathEditor.DirectoryList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ListBox.Resources>
    </ListBox.Resources>
    <ListBox.ItemTemplate>
        <DataTemplate>
            <StackPanel Orientation="Horizontal">
                <Image Name="indicatorImage" Source="/WindowsPathEditor;component/Resources/folder.png" />
                <TextBlock Text="{Binding SymbolicPath}" Margin="8,2,2,2"/>
            </StackPanel>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Exists}" Value="False">
                    <Setter TargetName="indicatorImage" Property="Source" Value="/WindowsPathEditor;component/Resources/folder_delete.png" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </ListBox.ItemTemplate>
</ListBox>
